# exec — 系统命令执行

在系统终端中执行 shell 命令。支持两种模式：

## return 模式（默认）
等待命令执行完毕，返回 stdout、stderr 和退出码。
适合：编译、安装、数据处理等需要拿到结果的场景。

可选参数 `timeout`：超过指定秒数后自动终止。

## no_return 模式
后台启动命令，不等待结果立即返回。
适合：启动服务、后台监听等不需要立即返回的场景。

> **注意**：此工具在用户的工作站上运行，可以访问文件系统和网络。
