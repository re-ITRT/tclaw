# read — 文件阅读

读取指定文件的内容。支持文本文件和图片文件。

## 文本文件
- `offset`：起始行号（从 1 开始），不指定则从开头
- `limit`：最多读取多少行（默认 2000）
- 超过 50KB 时自动截断，并在末尾显示 continuation 提示
- 截断后可指定 `offset` 继续读取

## 图片文件
自动检测图片格式（jpg/png/gif/webp），返回 base64 编码数据。
