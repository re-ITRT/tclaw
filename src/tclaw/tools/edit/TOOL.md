---
name: edit
description: "编辑文件（精确替换）。相对路径以工作区为根，绝对路径直接使用。"
---

# edit —— 文件精确替换

将文件中的精确文本替换为新内容。

## 路径规则

- **相对路径**：基于工作区根目录
- **绝对路径**：直接使用

## 要求

- 每个 `oldText` 必须在文件中出现且仅出现一次
- 换行符统一为 LF 后进行比较
