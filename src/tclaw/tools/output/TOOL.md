# output — 输出给用户 / 结束会话

向用户展示信息或结束当前轮次。支持三种模式：

## text 模式（默认）
纯文本输出。用于报告结果、回答问题等。

## figure 模式
输出图片。`path` 参数指定图片文件路径。

## end 模式
**结束当前会话轮次。** 调用后 LLM 循环自然停止，等待用户下一次输入。
最后的消息内容通过 `text` 参数传递。

> 这是与用户交互的主要方式。
