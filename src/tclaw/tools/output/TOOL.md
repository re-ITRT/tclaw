---
name: output
description: "回复用户。LLM 必须用此工具与用户交流，不可直接返回文本。支持 text/figure/end 三种模式。"
---

# output — 回复用户（LLM 的唯一对话出口）

**重要：这是 LLM 与用户交流的唯一方式。永远不要直接返回文本，必须使用此工具。**

支持三种模式：

## text 模式（默认）
纯文本输出。用于报告结果、回答问题等。

## figure 模式
输出图片。`path` 参数指定图片文件路径。

## end 模式
结束当前会话轮次。调用后等待用户下一次输入。
