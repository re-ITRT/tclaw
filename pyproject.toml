[project]
name = "tclaw"
version = "0.1.0"
description = "下一代 OpenClaw — AI Agent 网关与后端"
requires-python = ">=3.11"
dependencies = ["openai>=1.0.0"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
